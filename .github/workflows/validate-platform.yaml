name: Validate platform

on:
  pull_request:
    paths:
      - ".github/workflows/**"
      - "infra/**"
      - "k8s/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  terraform:
    name: Terraform
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: 1.16.0
          terraform_wrapper: false
      - run: terraform -chdir=infra/terraform fmt -check
      - run: terraform -chdir=infra/terraform init -backend=false -input=false
      - run: terraform -chdir=infra/terraform validate -no-color
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.5"
      - run: >-
          go run github.com/terraform-docs/terraform-docs@v0.20.0 markdown table
          --output-file README.md --output-mode replace infra/terraform
      - run: git diff --exit-code -- infra/terraform/README.md

  ansible:
    name: Ansible
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Install validation tools
        run: >-
          python -m pip install --disable-pip-version-check
          ansible-core==2.21.3 ansible-lint==26.8.0 yamllint==1.38.0
      - name: Check YAML
        run: >-
          yamllint -d '{extends: default, rules: {line-length: disable}}'
          infra/bootstrap infra/ansible k8s
      - name: Check Ansible style
        env:
          ANSIBLE_CONFIG: ${{ github.workspace }}/infra/ansible/ansible.cfg
        run: ansible-lint infra/ansible/playbooks infra/ansible/roles
      - name: Check Ansible syntax
        env:
          ANSIBLE_CONFIG: ${{ github.workspace }}/infra/ansible/ansible.cfg
        run: >-
          ansible-playbook --syntax-check --inventory localhost,
          infra/ansible/playbooks/bootstrap-k3s.yaml

  manifests:
    name: Kubernetes manifests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: azure/setup-kubectl@v4
        with:
          version: v1.36.2
      - run: kubectl kustomize k8s/platform/root > /tmp/platform-root.yaml
