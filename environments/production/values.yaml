environment: production

# Preserve the initial resource and selector names while the chart itself becomes
# the OCI-published hello-api artifact.
nameOverride: hello

image:
  repository: nginxinc/nginx-unprivileged
  tag: 1.27.5-alpine
  digest: ""
